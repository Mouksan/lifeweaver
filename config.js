// ═══════════════════════════════════════════
// CONFIG — константы и настройки по умолчанию
// ═══════════════════════════════════════════
//
// Это Этап 1 (Фундамент). Тут сознательно почти ничего нет —
// модель вселенных (mpreg/omegaverse/dragon/merfolk + конструктор)
// приезжает на Этапе 2. Пока задача — доказать, что расширение
// вообще подключается и per-chat хранение работает.

// Ключ в extension_settings И относительный путь до папки расширения
// (стандартный паттерн ST для third-party расширений).
export const extensionName = 'third-party/lifeweaver';

export const defaultSettings = {
    isEnabled: true,
    // Данные, привязанные к конкретному чату (per-chat), ключ — id чата.
    chatData: {},
};

// Дефолтная форма per-chat данных. note — временное тестовое поле,
// чтобы проверить, что каждый чат хранит свои данные отдельно.
export const defaultChatData = {
    note: '',
};
